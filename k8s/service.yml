apiVersion: v1
kind: Service
metadata:
  name: secsatn
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "secsatn"
    port: 8080
  selector:
    app: secsatn
