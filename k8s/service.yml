apiVersion: v1
kind: Service
metadata:
  name: hostiony
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "hostiony"
    port: 8080
  selector:
    app: hostiony
