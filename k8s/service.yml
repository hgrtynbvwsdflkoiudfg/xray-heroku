apiVersion: v1
kind: Service
metadata:
  name: rctiony
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "rctiony"
    port: 8080
  selector:
    app: rctiony
